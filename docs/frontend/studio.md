# AI Studio

Layout:
left configuration panel + main preview/workspace + generation history/context where appropriate.

The model selector must display capability-compatible models only.

Changing model dynamically changes supported controls.

Show estimated credits before generation.
Disable submit while reservation is pending.
Show queued/processing progress and allow cancellation when supported.
