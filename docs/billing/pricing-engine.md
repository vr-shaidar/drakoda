# Pricing Engine

Input:
model, operation, resolution, quality, duration, output count and other billable dimensions.

Output:
provider estimated cost, customer estimated price, estimated credits, pricing version.

Pricing calculation must be deterministic and auditable.

Store the exact pricing version used for each generation.
