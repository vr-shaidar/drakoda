# Subscriptions

Plans are data-driven.

Plan attributes can include:
monthly credits, concurrency, storage, max duration, max resolution, watermark, API access, commercial rights, priority and rate limits.

Subscription lifecycle:
trial -> active -> past_due -> cancelled -> expired.

Handle Stripe events idempotently.
