# Deployment

Development: Docker Compose.

Production requirements:
HTTPS, managed PostgreSQL or equivalent, Redis, object storage, secrets management, backups, monitoring, centralized logs, worker scaling and health checks.

Provide separate environments:
development, staging, production.

No secrets committed to Git.
