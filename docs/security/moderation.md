# Content Moderation

Moderation must be provider-aware and configurable.

Possible checks:
prompt moderation
input image/video moderation
output moderation where available
abuse/rate-limit signals.

Store moderation decision, rule/version, timestamp and result for auditability.
Do not expose provider internal moderation details unnecessarily.
