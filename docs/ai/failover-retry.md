# Retry and Failover

Retry transient failures only.

Before failover verify:
- equivalent capability
- input compatibility
- customer plan eligibility
- current model availability
- pricing
- provider health

Never silently switch to a materially different model without a policy allowing it and recording the selected fallback.
