# Image to Video

Flow:
upload/select source image -> validate format/size -> store asset -> select compatible model -> prompt/settings -> estimate credits -> reserve -> queue -> provider -> store video -> finalize billing.

Preserve the source asset relationship in the generation record.
