# Image Generation

Support:
- text-to-image
- image-to-image where provider supports it
- image editing
- inpainting/outpainting where supported
- variations where supported
- upscaling where supported

Persist prompt, model, settings, source assets, outputs, provider metadata and costs.

Use provider capability metadata to determine available UI controls.
